export interface IndustryData {
    walletAddress: string;
    industryName: string;
    city: string;
    latitude: string;
    longitude: string;
    verified?: boolean;
    carbonCaptured?: string;
}